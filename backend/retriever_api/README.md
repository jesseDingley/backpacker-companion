# Retriever API

To launch the Hybrid Retriever API, you need to (from the VM):

1. Activate the `/home/jessedingley/playground/pg_env` virtualenv.
2. `cd` into `/home/jessedingley/backpacker-companion`
3. Run `make launch-api`

The code in this folder will not run outside of the VM.